doctype html
html
  head
    block title
      title Express Shop
    link(href='/stylesheets/global.css', rel='stylesheet',type='text/css')
    block stylesheets
  body
    block header
      div#header
        h1 Express Shop
        a(href='/cart/') My Cart
        a(href='/myprofile/') My Profile
        if nosearch
        else
          form#search-form(method="get" action='/search') #[input(type="search" placeholder="Search products" name="searchQ")] #[input(type="submit" name="Enter")]
    block main